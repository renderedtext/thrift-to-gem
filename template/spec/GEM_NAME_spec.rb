require 'spec_helper'

describe GEM_MODULE_NAME do
  it 'has a version number' do
    expect(GEM_MODULE_NAME::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
